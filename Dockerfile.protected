FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn[standard] curl-cffi 2>/dev/null || true

COPY dummy_app.py /app/dummy_app.py

EXPOSE 5000

CMD ["uvicorn", "dummy_app:app", "--host", "0.0.0.0", "--port", "5000"]
